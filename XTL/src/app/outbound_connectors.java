package app;

import java.io.*;
import java.util.*;
import com.fasterxml.jackson.databind.*;
import com.fasterxml.jackson.dataformat.csv.*;

// enum for supported data outlets
enum Outlet {
    SPARKSQL
}

public class outbound_connectors {
    // placeholder for outbound connectors

    // SparkSQL does not require special connector because JSON file is supported

    // save file in outbound_data folder
}
